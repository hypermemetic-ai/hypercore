accepted: worlds/self_model_world.py @422 — the multi-commit gate fixture (a self-committing worker) must live in the carried world module for its red->green to be honest -- only <cap>_world.py rides onto the base checkout, so it cannot factor into the un-carried _new_verb_fence helper
accepted: engine/worlds/self_model_world.py @422 — the multi-commit gate fixture (a self-committing worker) must live in the carried world module for an honest red->green -- only <cap>_world.py rides onto the base checkout, so it cannot factor into the un-carried _new_verb_fence helper
accepted: worlds/worker_world.py @477 — the proposer-layer fixtures (a worker applies only an architect-proposed delta: proposed/built/unproposed/held/awaiting-delta/trivial-proposed/no-author-from-scratch) must live in the carried world module for an honest red->green -- only <cap>_world.py rides onto the base/tip checkout, so they cannot factor into an un-carried helper (the self_model_world @422 precedent)
accepted: engine/worlds/worker_world.py @477 — the proposer-layer fixtures (a worker applies only an architect-proposed delta: proposed/built/unproposed/held/awaiting-delta/trivial-proposed/no-author-from-scratch) must live in the carried world module for an honest red->green -- only <cap>_world.py rides onto the base/tip checkout, so they cannot factor into an un-carried helper (the self_model_world @422 precedent)
