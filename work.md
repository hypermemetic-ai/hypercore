# work

## the log reads at a glance

- ask: from your words, twice now — log entries led with the hash and ran flat; then the yellow was hideous and the filler words wasted the line. now every log line — the record (g) and the since-you-last-acted head both — leads with the act in bold, your own acts in cyan, the rest dim, hash and age trailing at the end. non-load-bearing words are cut on display ("the", "a", "an", "in the interface"), and the room they free shows more of each entry's detail. the queue's accept labels still wear the old yellow — say the word if it goes everywhere.
- try: relaunch hyper (q, then the menu) — g: "approved by operator" leads in cyan with more of the detail visible, hashes trail dim; the hyper open now wears the old code
- blocks: until accepted, the record stays as the version you called better-but-not-there
- state: awaiting acceptance
- since: 2026-06-11

## seamless operation while the machine writes

- ask: from your words — the red line and the speech guard cut you off exactly when the machine is mid-write, because the guard is all-or-nothing: any dirty ledger blocks every act. the sober rundown: the collision is two writers on one tree, and it is real — an act you land on a half-written ledger can race the machine's next write; full isolation buys a clean tree but moves the cost into merge machinery and lagged answers.
- options: scope the guard per act and soften the line — speech blocks only while words.md itself is dirty, ratification only on a dirty intent or queue, cards only on a dirty work.md; the line reads dim ("the machine is mid-write") and red is reserved for a dead session's leavings, the one state that needs you . isolate the machine in its own git worktree, merged atomically — you never see a dirty tree, but answers land only at the merge, conflicts become real machinery, and the live tick reads stale until then . leave it — the red stands, and the machine just commits faster
- blocks: until settled, every machine build flashes red at you and refuses your speech — the longer the build, the longer the lockout
- state: awaiting your decision
- since: 2026-06-11
