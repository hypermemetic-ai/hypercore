# work

## work view: symbols gone, node text sectioned and legible

- ask: you liked the collapse/navigation but not the ▸/▾ symbols, and asked to make the text under the nodes clearly legible and clean — the sectioning didn't come across, it read as one big chunk.
- try: relaunch hyper (q, reopen — it reads its own code only at launch). Open the work view (w), unfold hypercore or a graph, enter on a node: the ▸/▾ marks are gone, and the opened block now reads as sections — each field under its own dim label (ASK, CHECK, RESULT, …), the body in full contrast (no longer dim), a blank line between — so where one section ends and the next begins is obvious at a glance. Going forward the machine also writes node fields cleaner (one concern per field, no walls), recorded in SKILL.md.
- state: awaiting acceptance
- blocks: nothing — a display change plus a writing discipline; fully reversible, nothing compounds while it waits.
