# work

## the main screen wears the standing-work row

- ask: from your words, in your words — the main screen now opens with "standing work: N . awaiting acceptance: N . open subgraphs: N . w opens the view" ("your" stays cut — the folded work view's summary speaks the same format), and the since-you-last-acted rows beneath it follow the refined log structure at full width: substance, act, hash, age, striped.
- try: relaunch hyper (q, then the menu) — the first row under the title reads the counts and names w; the move rows beneath align like the log
- blocks: nothing — display only
- state: awaiting acceptance
- since: 2026-06-11

## the jumps get their own keys

- ask: from your words — some screens wanted the jumps back, and the main page's home/end deserved generalizing. space/b keep walking one line; pgup/pgdn jump a screenful and home/end jump to the edges, in every view — selection on the main page and in w, scroll in cards, exchanges, documents, and the log.
- try: relaunch hyper (q, then the menu) — g: pgdn leaps, home tops, end bottoms; on the main screen home/end snap the selection to first and last
- blocks: nothing — keys only
- state: awaiting acceptance
- since: 2026-06-11

## no-words summons run fable at low effort

- ask: your pick, option 3 of the routing card, executed — the mini file now summons the same model as the full machine at low effort instead of sonnet: one model, one cache, one behavior, most of the speed, no cross-model risk. the commit-only-never-correct bar and the escalation path stay exactly as accepted.
- try: leave the record dirty with no words waiting and relaunch — the bare true-up summon runs fable at low effort (the mini file is the one visible setting; .hyper-machine.log holds the run)
- blocks: nothing runs unbacked — the settled card (957d80b) is the decision
- state: awaiting acceptance
- since: 2026-06-11
