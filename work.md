# work

## open work summons the machine

- ask: your pick on the what-runs-when-work-is-open card (option 3, 57f6f89) — when open graph nodes stand with no machine running, no words waiting, and a clean record, hyper summons the machine to them with a work instruction: fold what has met its condition, work what a node's ask authorizes, leave what waits on you. Progress is the guard against looping spend: one summon per record head — until a session moves the record, the next summon is yours (m, which now also fires on open work alone). Sessions chain while they make progress; a session that has nothing to move says why in .hyper-machine.log and the chain rests until the record moves again.
- try: relaunch hyper (q, then the menu) — no open graph stands right now, so nothing fires; the next graph that opens summons the machine by itself, "open work stands — the machine is summoned to it" flashes and lands in .hyper.log, and m works on open work even with no words and a clean record
- blocks: nothing — until a graph opens, the mechanism is dormant
- state: awaiting acceptance
- since: 2026-06-11

## the main screen wears the standing-work row

- ask: corrected three times on your words (2026-06-11, the last: you don't like the look of the log lines on the main screen — it ought to look the best, immaculate, never tarnished) — the main screen now speaks in calm prose: the since-you-last-acted rows are full sentences, dim, no stripes, no hash, no act or age columns; the standing-work line below the queue reads "standing work: N . awaiting acceptance: N . open subgraphs: N . w opens the view"; the last line under it is quiet prose too — "last  <what happened> . <age>", no hash. Columns, hashes, and stripes live in the log view (g), where reference reading belongs.
- try: relaunch hyper (q, then the menu) — the since-rows and the last line read as plain sentences; nothing tabular anywhere on the main screen
- blocks: nothing — display only
- state: awaiting acceptance
- since: 2026-06-11

