# work

## where work that is not about hypercore lives

- ask: from your what-now word — your structure statements already lean one way: intent lives in the graph's folder, a folder holds one execution graph, this repo is hypercore's folder. an outside project would carry its own intent.md and graphs as a few extra files in its own repo, behaving naturally, and hypercore would keep a link so the interface reads one queue — your cards and words stay here in every option. the alternative keeps all documents in hypercore, untouched target repos, but the record is then versioned apart from the work it describes. whichever you pick, the link machinery itself is not built now — it waits for the first real outside ask, per the bar
- options: with the project — its folder carries its intent and graphs in its own repo, hypercore keeps a registry of linked folders the interface reads . with hypercore — outside graphs live here under work/<name>/, target repos stay clean but the record lives apart from the work . don't settle yet — leave the question until a real outside ask forces it with a concrete case in hand
- blocks: taking on any ask that is not about hypercore — none stands yet, so nothing compounds while this waits
- state: awaiting your decision
- since: 2026-06-11
