# work

## model routing as a structural element

- ask: from your words — the research stands in work/model-routing/research.md (d on its line under w): the only unconditional free lunch, speculative decoding, already runs provider-side inside Anthropic's serving stack; the conditional one — route down what a mechanical check can catch — is the mini, already built and accepted. learned routers (RouteLLM: 95% of frontier quality at ~26% of the calls) pay for their savings with a 3–5% quality gap and misroutes — wrong shape for hypercore, where a misrouted answer to your words costs an operator round-trip, the scarcest thing in the system. words never route down; prompt caches are per-model, so routing belongs only at session boundaries, where the summons already sit. what remains is whether to widen the structural route — quality risk, so yours.
- options: leave it — the mini's class (bare true-ups) stays the only down-route; revisit when a new mechanically-verifiable class appears . widen the mini's class — every no-words summon (true-ups, folds, ledger bookkeeping) goes to sonnet under the same commit-only-escalate bar; faster routine ticks, a small new surface where the bar must catch what sonnet gets wrong . an effort tier instead — keep fable for everything but run no-words summons at lower effort: one model, one cache, one behavior, most of the speed, no cross-model risk
- blocks: nothing runs unbacked — the mini keeps working as accepted while this waits
- state: awaiting your decision
- since: 2026-06-11
