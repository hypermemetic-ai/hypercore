# work

## trak OIDC path — the version set it needs was never published

- ask: You decided (option 2, commit b49ef49) to clone + build plexus-idp as the real OIDC issuer — the cost on the card was "workspace version bump (coordinated checkouts) + a second heavy build." Executing it, the version bump turned out not to be a checkout at all. trak's OIDC branch (feature/UT-wave3-oidc-cutover) requires plexus-core ^0.6 and plexus-transport ^0.4, and no ref of those repos carries them: plexus-core's main and every tag stop at 0.5.3, plexus-transport at 0.3.0 — cargo refuses outright (`failed to select a version for the requirement plexus-core = "^0.6"`, candidate found 0.5.3). The UT-1/UT-wave3 wave was published for auth-core (0.2.0) and macros (0.6.0) but NOT for core or transport; they sit at the older versions, which still pin auth-core 0.1 (itself conflicting with trak's auth-core 0.2). I verified the API isn't the gap — core 0.5.3 already carries the AuthContext re-export trak leans on (AUTHZ-CORE-CRATE-1) — the gap is purely the missing 0.6/0.4 releases. So the OIDC path can't build before plexus-idp ever enters the picture. The decision rested on a cost that's now proven false; the fork is yours:
- options: point me at the real version set — if plexus-core 0.6 / plexus-transport 0.4 exist somewhere you can reach (a private branch, another machine, a registry), name it and the OIDC path resumes exactly as costed (clone+build idp, build, confirm a write); cheapest IF the artifacts exist, costs you locating them . forge core 0.6 / transport 0.4 locally — bump and re-pin the cloned plexus-core (to 0.6, auth-core 0.2) and plexus-transport (to 0.4), fix whatever the auth-core 0.1→0.2 jump breaks, then continue the OIDC path; stays fully local but your backend then leans on hand-forged plexus libraries that match no release — the bespoke-material cost option 2 chose plexus-idp to avoid, now pushed down into core/transport, plus an unknown amount of breakage . fall back to the one-line session_id patch on branch A — the legacy set already builds and ran on :44107 (core 0.5.3 / auth-core 0.1); the one-line patch makes writes round-trip and you've seen it. Writes work TODAY, no issuer, no version forging; cost: hyper relies on a one-line-patched trak. You weighed this against OIDC and chose OIDC — but against a cost far lower than what the OIDC path actually carries . pause the switch — the filesystem backend keeps running untouched, nothing compounds; revisit when the real plexus version set is reachable
- state: decided (2026-06-13) — option 3 — fall back to the one-line session_id patch on branch A — the legacy set already builds and ran on :44107 (core 0.5.3 / auth-core 0.1); the one-line patch makes writes round-trip and you've seen it. Writes work TODAY, no issuer, no version forging; cost: hyper relies on a one-line-patched trak. You weighed this against OIDC and chose OIDC — but against a cost far lower than what the OIDC path actually carries
- since: 2026-06-13
- blocks: the standup's write-confirmation and through it the whole switch — the client (synapse-cc) and the document→facet mapping can't be built or tested against a trak that won't build or accept writes. Nothing compounds: the filesystem backend keeps running untouched, so hypercore loses nothing while it waits.

