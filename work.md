# work

## prepare for a very big unit of work

- ask: a logistics-platform-sized ask will outrun single episodes immediately. the doctrine for that is ratified — intent.md's work section: execution graphs, as-needed seams, every child born with its ask and its named check, weak episodes fold shallow — but it is intent, not machinery. work.md is a flat list; there is no folder-per-graph, no folding, no recorded checks. rebuild discipline forbids machinery before its absence blocks work twice, and epoch 1 burned precisely for building ahead of need. how do we prepare?
- options: let the doctrine run (no machinery now; the first big ask ratifies its intent first and the machinery gets built as accepted slices at the moment its absence blocks — the first days of the real workload include building the bench while standing at it) . rehearse now (you bring a real, wanted ask big enough to force decomposition; the blocks happen honestly at low stakes and the machinery exists, accepted, before the platform arrives — costs your attention now) . build now on your word (your word waives the two-blocks bar; graph machinery lands as slices with acceptance cards — risk: machinery shaped by guesses instead of live work, the epoch-1 failure)
- state: awaiting your decision
- since: 2026-06-11

## push epoch 2 to hypermemetic-ai/hypercore

- ask: the remote exists and is public. it still holds the old epoch-1 hypercore (19 commits, no shared root with ours), which I will keep as branch epoch-1 before replacing main — nothing lost. one call is yours before anything leaves this disk: every epoch-2 commit names ggpcv3@use.startmail.com as author, where epoch 1 was published under a github noreply address. push as-is, or rewrite authorship first?
- options: push as-is (your address goes public in all 259 commits — unretractable once cloned or indexed) . rewrite then push (the author on every commit becomes the noreply address; every commit id changes, content, messages, and dates stay; future commits set to match) . hold (nothing pushed; one command removes the remote)
- state: awaiting your decision
- since: 2026-06-11
