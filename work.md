# work

## trak OIDC path — which issuer, and the cost it actually carries

- ask: You decided to move to the OIDC branch (UT-wave3) and stand up an issuer — set. Executing it, two things surfaced that change the cost from how "move to a branch" sounded. (1) UT-wave3 doesn't drop into the current workspace: it pins plexus-core 0.6, plexus-transport 0.4, plexus-auth-core 0.2 (with the oidc-reqwest JWKS fetcher) — all higher than what's cloned now (0.5.3 / 0.3.0 / v0.1.0), so the WHOLE plexus workspace must move to the matching UT-1/UT-W3 version set before trak builds; that's a coordinated multi-repo checkout that may hit version-skew walls like the auth-core one did, plus a full rebuild. (2) trak then validates RS256 via OIDC discovery + JWKS against --oidc-issuer (default http://localhost:4461, audience "plexus:trak"); an issuer must serve /.well-known/openid-configuration + a JWKS and mint RS256 tokens with those claims. plexus-idp is the reference issuer but isn't cloned. The issuer is the resource fork, and it's yours:
- options: stand up a minimal local RS256/JWKS issuer — a small hand-written service (RS256 keypair, serve discovery + JWKS, mint a plexus:trak token); lightest, fully local, no second heavy build — but bespoke material your backend-auth leans on . clone and build plexus-idp — the real reference issuer at :4461, faithful to production and exactly what trak expects, but the full OIDC stack you deferred: clone + heavy Rust build + a second daemon to keep alive . fall back to the one-line patch on branch A — given the real OIDC cost above (workspace version bump + issuer standup), the session_id fix still gets writes TODAY with no issuer and no version bump, and you've seen it round-trip; I hold it as the honest fallback now that the OIDC price is visible
- state: decided (2026-06-13) — option 2 — clone and build plexus-idp — the real reference issuer at :4461, faithful to production and exactly what trak expects, but the full OIDC stack you deferred: clone + heavy Rust build + a second daemon to keep alive
- since: 2026-06-13
- blocks: the standup's write-confirmation and through it the whole switch — the client (synapse-cc) and the document→facet mapping can't be built or tested against a trak that won't accept writes. Nothing compounds: the filesystem backend keeps running untouched, so hypercore loses nothing while it waits.

## Claude plan utilization — no rate-limit state on disk; what's buildable is token volume

- ask: You decided option 1 — read Claude Code's own usage/rate-limit state from disk. Executing the look, it isn't persisted: .claude.json holds only config, there is no usage/quota file under ~/.claude, the sessions/ entries carry only pid/sessionId/cwd, and across 60 session transcripts the only rate-limit field is `error.rateLimits = null`. Claude Code reads the live anthropic-ratelimit-unified headers (the rolling 5-hour + 7-day windows with reset times) at runtime but does NOT write that status to disk. What IS on disk is per-message token usage inside the transcripts (input/output/cache tokens, service_tier, timestamps) — raw activity, not your plan's cap %. So option 1's premise doesn't hold; the choice is back to you with the real landscape:
- earlier fork (resolved — you chose true plan-% over these, fc43fd2): build from the transcript token logs — token + message volume per session (5h) / day / week / month, buildable now with no network, but "tokens used" not "% of plan cap" . go for true plan-% utilization — the number you actually want, a runtime/API capture not a disk read . pause — leave the queue screen as is
- options: authorize a credential'd endpoint probe — I use your stored OAuth creds (~/.claude/.credentials.json) to look for an Anthropic subscription-usage endpoint and build on it if one returns plan-%; network + your credentials, and if none exists I report back . investigate the harness-hook route first — no creds: check whether Claude Code exposes the ratelimit headers to a Stop-hook/wrapper, build on that if it does . take the token-volume fallback now — skip the hunt, build the buildable readout (tokens/messages per 5h/day/week/month) today
- update (2026-06-13, source fork restored): true plan-% is settled (you picked it over the token-volume fallback and pause, fc43fd2). The real open decision is the SOURCE for that number — the three paths now in options. They were first surfaced as a sub-fork (8412c0f) but under a field hyper's decide card doesn't read, so the picker kept showing the resolved top-level fork; that's why "true plan-%" was recorded twice (fc43fd2, a24f658) and the state briefly read "decided/executable." It is NOT executable — I touch no creds and no endpoint until you pick the source ("its own decision before I touch it"). Moved the live fork into options so the card offers it.
- state: decided (2026-06-13) — option 1 — authorize a credential'd endpoint probe — I use your stored OAuth creds (~/.claude/.credentials.json) to look for an Anthropic subscription-usage endpoint and build on it if one returns plan-%; network + your credentials, and if none exists I report back
- since: 2026-06-13
- blocks: nothing compounds — additive to the queue screen, independent of the trak switch; it waits only on which source feeds it.

## improve the STS2 spirebird overlay — what to surface to actually improve play

- ask: Looked at what's running (your word, 2026-06-13). The overlay is the spirebird_stats mod (author "Jorbs", v1.0.0) in your STS2 mods folder — a DLL that pulls win-rate / pick-rate / Elo / WAR from spirebird.com and shows them on card-reward, relic, and event tooltips; it caches two cohorts, midA10 (A10 est. >50% win) and strongA10 (>75%), Tab rotates them, your-own-runs comparison is OFF (includeLocal/localStats false). So your "WAR A10 >50%/>75%" is those two cohorts, and the data source is spirebird.com (not the jorbs spreadsheet I first guessed). The lever: the cached data holds far more than it shows (per card: elo, war, pickWar, pwarPerPick, pickRate, winRate, baselineWhenSeen, per-character splits), so most of "what else improves play" is a config.json toggle, not a build. (Log snag, for you to glance at: the engine's recursive mod scanner mistakes cache_*A10.json for mod manifests and throws "missing 'id' field" — confirm the overlay still renders; if it does, harmless noise.) The fork is yours:
- options: config-tuning pass — I propose a concrete spirebird_stats config.json diff (surface decision-signal fields like pickWar-per-pick / baselineWhenSeen over raw Elo; switch on your-runs-vs-community; cohorts matched to the ascension you actually play), you approve it, edit + relaunch — near-zero cost, captures most of the play gain because the data is already cached . companion build — build what spirebird does NOT track (pace-vs-your-own-winning-runs from save data, live-fight "outs") — a real project with its own decisions, properly its own repo . guidance only — I lay out which spirebird fields most improve decisions and you tune config yourself, no card kept
- state: awaiting your decision — config-tuning pass / companion build / guidance only
- since: 2026-06-13
- blocks: external to hypercore — nothing compounds; the overlay runs as-is meanwhile. This is your external STS2 project, carded here in hypercore's work.md for now; its proper home is its own linked-project repo (that registry isn't built yet — say the word and it becomes the first linked project).

