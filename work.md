# work

## stock trak can't write facets on this branch — the write path is yours

- ask: Executing your foothold (option 2: build trak, run it single-user / legacy-HS256), I built the staged workspace and stood trak up on :44107 — register, login, and identity round-trip over a direct JSON-RPC client (the "direct and fast" path), and the daemon serves. But your acceptance bar — "I confirm writes work on that path before relying on it" — is NOT met by stock trak on this branch. A facet create fails: "create requires an authenticated tenant". Traced to source: this branch (feature/AUTHZ-TENANT-GATE-trak-facets) has a tenant gate (src/tenant_gate.rs:138) that rejects any caller whose AuthContext is not `is_authenticated()` — which needs a non-empty session_id — yet trak's own auth (src/auth.rs TrakAuth::try_jwt/try_api_key) builds every over-the-wire AuthContext with an EMPTY session_id ("no session ID for JWT"). The auth module and the tenant-gate feature on this branch are mutually inconsistent: every wire client (JWT cookie or bearer, or api-key) is treated as anonymous, so ALL facet writes (create/update/delete/move/link) fail. Reads of public facets and identity register/login still work. I verified the minimal fix — give try_jwt a non-empty session_id (`jwt:<sub>`) — makes a facet create→get round-trip PASS (id, title, body all match); then reverted it, leaving trak stock, because adopting it makes hyper rely on a patched trak — your call. Build + run are done and reproducible from clean clones (one wall en route: the auth-core clone sat on 0.2.0 while trak+plexus-core pin 0.1, fixed by checking out its v0.1.0 tag).
- options: adopt the one-line auth fix — patch TrakAuth to set a non-empty session_id (try_jwt + try_api_key) and keep this branch (the legacy HS256 single-user path you chose); demonstrated to make writes round-trip, lightest path to a writable trak today; cost is hyper relies on a locally-patched trak that must be re-applied if trak is re-pulled, and the patch is mine, not upstream . target a stock trak branch without this wall — trak's main may predate or properly integrate the tenant gate; stock, no local patch, but it may not be the legacy-HS256 single-user path you pictured, and needs a quick check of which branch writes cleanly before relying on it . pause the switch here — leave trak built but not relied on; the filesystem backend keeps running untouched, nothing compounds, and the client + mapping work stay blocked until the write path is settled
- state: awaiting your decision
- since: 2026-06-13
- blocks: the standup's write-confirmation and through it the whole switch — the client (synapse-cc) and the document→facet mapping can't be built or tested against a trak that won't accept writes. Nothing compounds: the filesystem backend keeps running untouched, so hypercore loses nothing while it waits.
