# work

## thread auto-rename — the machine names the threads it answers

- ask: you changed your mind (2026-06-13) — you do want auto thread rename, but only when you have not renamed a thread by hand.
- try: no relaunch needed — this is how the machine answers, not hyper code. From now on, when the machine answers a thread still wearing its raw first-words heading, it rewrites the heading to a short, true topic (the summary the heuristic couldn't reach). Speak a new thread and watch its label sharpen once the answer lands. Rename any thread by hand (r) and the machine will never overwrite your label — a heading off its first-words default is left alone, whoever set it. (I did not retroactively rename the threads already open this session: a running hyper keys an open thread by its heading, so renaming one you're reading would snap your view home.)
- state: awaiting acceptance
- blocks: nothing — naming is cosmetic and reversible (r renames, git keeps every word); nothing compounds while it waits.
