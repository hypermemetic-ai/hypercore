# work

## the log reads at a glance

- ask: from your words — log entries led with the hash and ran the subject flat, so the act drowned in the noise. now every log line — the record (g) and the since-you-last-acted head both — leads with the act: the subject's head, up to its first ": " or " — ", reads first and bold, the rest stays dim, and the hash and age trail at the end, where reference, not reading, looks for them. your own acts wear the accent color; the machine's read bold. symbols stayed out — two weights and one color carry it.
- try: relaunch hyper (q, then the menu) — g: "the queue ratifies linked intent" and "accepted by the operator in the interface" lead their lines, hashes trail dim; the queue's head reads the same way; the hyper open now wears the old code
- blocks: until accepted, the record stays hash-first and flat — what happened still has to be dug out of every line
- state: awaiting acceptance
- since: 2026-06-11
