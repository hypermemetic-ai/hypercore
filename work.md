# work

## hypercore's line counts folded subgraphs by name

- ask: from your words — "hypercore lists 1 graph when really we mean 1 (folded) subgraph I think." the folded hypercore line said "1 graph, 0 open", naming hypercore's children as if they were peers and burying the fold in a zero. now the line counts them as subgraphs and, when none is open, says so plainly: "1 folded subgraph"; with any open it reads "N subgraphs, M open". the queue's counter says "open subgraphs" to match. the term goes no deeper than the counts — graph lines, headers, and help prose still say graph; say the word if you want the rename carried through.
- try: relaunch hyper (q, then the menu) — w: hypercore's folded line now ends "1 folded subgraph"; the hyper open now wears the old code
- blocks: until accepted, the folded line keeps reading "1 graph, 0 open" — the children read as peers and the fold stays a zero
- state: awaiting acceptance
- since: 2026-06-11
