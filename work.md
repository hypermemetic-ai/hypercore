# work

## the fold rides the acceptance

- ask: a slice that is a graph's folding condition now folds the graph in the very act of acceptance — the machine writes `graph: <name>` on the card, and y lands one commit carrying both the card's exit from work.md and the root's fold (result + state folded); built on your word "let's do it now, once is proof enough", after graph-machinery's fold lagged its acceptance until you asked
- try: a running hyper executes its launch-time code — relaunch first; the proof arrives with the next graph: its acceptance card will carry the graph field, and accepting it will fold the graph in the same commit, no stale "awaits acceptance" left standing; this card itself names no graph and accepts plain
- state: awaiting acceptance
- since: 2026-06-11
