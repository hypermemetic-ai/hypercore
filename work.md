# work

## accept confirms like every release
- ask: accepting a slice is a then y, the same confirm bar as approve, cut, and fold — no release in the interface is a single unconfirmed keypress
- state: built, awaiting acceptance — a on an acceptance card now raises the confirm bar; y releases the slice, esc keeps it. CLAUDE.md records the operator's word; the same line in .claude/skills/hypercore/SKILL.md is blocked to this session by harness permissions and still reads "frictionless act"
- since: 2026-06-11
- try: press a on any acceptance card — the bar asks for y before anything lands; esc keeps the slice
- waits on: one act on its acceptance card, in a freshly started hyper
