# work

## plan-% moved to the foot of the home screen, compact

- ask: you asked (2026-06-13) to move the plan-use % off the queue's flow into the empty space at the bottom of the screen, and make it compact, clean, legible at a glance.
- try: relaunch hyper (q, then reopen — the running copy reads its own code only at launch). On the home screen the plan-% now sits on its own line in the empty space just above the footer rule, no longer trailing "latest:". It reads compact — e.g. "plan · 5h 12% (2h05m) · 7d 8% (3d4h) · opus 5%": percentages first, each reset in bare parens, no "resets" word and no subscription tag; a stale readout still shows its age.
- state: awaiting acceptance
- blocks: nothing — a display-only move; nothing compounds while it waits.
