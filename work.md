# work

## stock trak can't write facets — and no stock branch fits both your constraints

- ask: You decided option 2 — target a stock trak branch without the write wall. Executing it, the premise doesn't hold: trak has no `main`. Its remote carries exactly two branches, and the one you're on (feature/AUTHZ-TENANT-GATE-trak-facets) IS the default (origin/HEAD). Recap of the wall on this branch: the tenant gate rejects any caller that is not `is_authenticated()` (needs a non-empty session_id), but TrakAuth mints every wire AuthContext with an EMPTY session_id — so all facet writes fail (reads + identity work); I verified a one-line session_id fix makes a create→get round-trip PASS, then reverted to stock. The only other branch, feature/UT-wave3-oidc-cutover, is the OIDC cutover: it FIXES the write wall (mints a real session_id) but removes the local HS256 path entirely ("jwt_secret no longer appears in the validation path; HS256-minted tokens are NOT accepted") and validates RS256 against an OIDC issuer's JWKS — it requires --oidc-issuer (plexus-idp or a compliant IdP). So NO stock branch is both writable AND legacy single-user: branch A is legacy-single-user but walled, branch B writes but needs plexus-idp — the exact thing you chose to skip. The real landscape is back to you:
- options: adopt the one-line auth fix on this branch — set a non-empty session_id in TrakAuth; the option you declined, but it is the ONLY way to keep legacy single-user / no plexus-idp AND get writes; demonstrated to round-trip, cost is a locally-patched trak that must be re-applied if trak is re-pulled . move to the OIDC branch (UT-wave3) and stand up an issuer — stock trak that writes, but this is the original foothold option-1 you set aside: it reverses "skip plexus-idp" and brings up the OIDC stack you deferred to the network phase (a minimal local IdP/JWKS could be lighter than full plexus-idp, but it is still more than no-idp) . pause the switch — leave trak built on branch A, filesystem backend untouched, nothing compounds, client + mapping stay blocked until the write path is settled
- state: decided (2026-06-13) — option 2 — move to the OIDC branch (UT-wave3) and stand up an issuer — stock trak that writes, but this is the original foothold option-1 you set aside: it reverses "skip plexus-idp" and brings up the OIDC stack you deferred to the network phase (a minimal local IdP/JWKS could be lighter than full plexus-idp, but it is still more than no-idp)
- since: 2026-06-13
- blocks: the standup's write-confirmation and through it the whole switch — the client (synapse-cc) and the document→facet mapping can't be built or tested against a trak that won't accept writes. Nothing compounds: the filesystem backend keeps running untouched, so hypercore loses nothing while it waits.

## Claude plan utilization — no rate-limit state on disk; what's buildable is token volume

- ask: You decided option 1 — read Claude Code's own usage/rate-limit state from disk. Executing the look, it isn't persisted: .claude.json holds only config, there is no usage/quota file under ~/.claude, the sessions/ entries carry only pid/sessionId/cwd, and across 60 session transcripts the only rate-limit field is `error.rateLimits = null`. Claude Code reads the live anthropic-ratelimit-unified headers (the rolling 5-hour + 7-day windows with reset times) at runtime but does NOT write that status to disk. What IS on disk is per-message token usage inside the transcripts (input/output/cache tokens, service_tier, timestamps) — raw activity, not your plan's cap %. So option 1's premise doesn't hold; the choice is back to you with the real landscape:
- options: build from the transcript token logs — real token + message volume bucketed by session (rolling 5h) / day / week / month from the .jsonl usage fields; honest activity-at-a-glance, buildable now with no network, but it is "tokens used," not "% of your plan cap" (Claude doesn't publish the caps as numbers) . go for true plan-% utilization — capture the live anthropic-ratelimit-unified status (five_hour/seven_day + resets) or call an Anthropic subscription-usage endpoint with the stored OAuth creds; this is the number you actually want, but it is a runtime/API capture not a disk read, depends on an endpoint hyper can reach, and uses your credentials — its own decision before I touch it . pause — leave the queue screen as is
- state: decided (2026-06-13) — option 2 — go for true plan-% utilization — capture the live anthropic-ratelimit-unified status (five_hour/seven_day + resets) or call an Anthropic subscription-usage endpoint with the stored OAuth creds; this is the number you actually want, but it is a runtime/API capture not a disk read, depends on an endpoint hyper can reach, and uses your credentials — its own decision before I touch it
- since: 2026-06-13
- blocks: nothing compounds — additive to the queue screen, independent of the trak switch; it waits only on which source feeds it.

