# work

## thread auto-rename keeps refining; a manual rename locks it

- ask: you said yes (2026-06-13) to the machine refining a thread's label as the thread grows, not just naming it once — and noted the cost is low.
- try: relaunch hyper (q, reopen — it reads its own code only at launch). The machine now rewrites a thread's heading to a current topic every time it answers, so the label tracks where the thread has gone. Rename any thread by hand (r) and it locks — the machine never overwrites your label again (the rename stamps a hidden marker). And refining is now snap-safe: renaming a thread no longer throws your open view back to home, because an open thread is keyed by its first words, not its heading. (I held off renaming the threads already open this session — the running copy still keys on the heading, so snap-safety only arrives with the relaunch.)
- state: awaiting acceptance
- blocks: nothing — naming is cosmetic and reversible; nothing compounds while it waits.
