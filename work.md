# work

## what runs when work is open

- ask: from your words — nothing keeps sessions running on open work; summons fire on words alone (speech summons the machine, launch summons it if words wait, one mid-answer resummon catches words that land during an answer). The item behind your question wasn't even waiting on work: model-routing's check was met the moment you accepted the effort-tier slice, and only the fold never happened — the same lag graph-machinery hit, so this gap has now bitten twice, which is the rebuild bar. Folded now (426230b); open subgraphs read 0. What remains is whether anything should run unattended on open work — that commits your spend, so it's yours.
- options: a standing order, no machinery — every summoned session re-reads open graphs before it stops, folds what is met and works what its words already authorize; open work still moves only when something else summons a session . hyper surfaces fold-ready roots — the interface checks open roots each tick and puts a line on the queue when one's nodes are all done; the fold stays your act, no unattended spend . unattended summons — hyper summons the machine when open nodes sit unworked and no words wait; work moves by itself, sessions run and spend without you watching . leave it — open work waits for the next words, and stale folds wait for someone to ask
- blocks: nothing runs unbacked — no graph is open right now; the gap next bites when one reaches its fold or sits unworked
- state: awaiting your decision
- since: 2026-06-11

## the main screen wears the standing-work row

- ask: corrected twice on your words (2026-06-11 — you meant the line below the queue, and the log line below that was part of the original ask) — the line below the queue is the one standing-work line, reading "standing work: N . awaiting acceptance: N . open subgraphs: N . w opens the view" ("your" stays cut), and the last-commit line below it now follows the refined log structure like every other log row: substance leading at normal weight, the act dim in its own column, hash and age right-aligned (the "last" label and plain-dim text are gone).
- try: relaunch hyper (q, then the menu) — nothing above the since-you-last-acted head; one standing-work line sits below the queue, and the line under it reads as a log row, not "last  …" in flat dim
- blocks: nothing — display only
- state: awaiting acceptance
- since: 2026-06-11

