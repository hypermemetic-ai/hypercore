# work

## main screen: queue + threads as two panels

- ask: the home screen stacks THE QUEUE above THREADS as two dim one-word captions in a single column, and the selection bar runs continuously across both as one list — so they read as one list with two labels, not the two principal, deliberate elements they are. How to make them read as two pillars with intentionality (you floated two panels).
- options: side-by-side panels — queue left (decisions), threads right (the open channel), one vertical rule between, ←/→ moves focus between the panels and j/k within the focused one; the meta lines (since-you-acted, standing-work, latest, plan-%) become a full-width footer band; echoes the two-column work view you just accepted and uses the horizontal space, at the cost of the most layout work and a focus model . stacked but framed — keep the vertical order but give each region an accent header, a rule beneath, and generous padding so they read as two deliberate spaces; cheapest and safe on any width, but neither uses the horizontal space nor matches the work view . side-by-side plus a full home recomposition — the panels of option 1, and with them a rethink of where since-you-acted / latest / plan-% sit, the typography and the rhythm, for one calm composed screen; most ambitious and most material
- state: awaiting your decision
- since: 2026-06-13
- blocks: nothing depends on it — the home screen works as is; this is a legibility-and-polish slice, so the only cost of waiting is the main screen reading as one list a while longer.
