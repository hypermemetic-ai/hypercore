# work

## two-column work view — list left, panel right

- ask: the layout you confirmed (option 1), now revised for the two reservations you raised. Unfolding hypercore — or a linked project, or a graph — shows a compact entry list on the left and the selected entry's full content in a panel on the right, divided by a rule; j/k moves the selection and the panel follows it, no inline expand. Both your points are addressed: each left line now leads with a type sigil — ▸ an open subgraph you can descend, ▪ a folded one, · a terminal entry or node — and a short status tag (a subgraph's open/total, a card's decide/accept, a node's open/done), so the kinds read apart at a glance again; and rather than widen the column, titles are capped at generation (the rule is now in the standing orders) with the long trak-backend headings renamed. enter still drops into a graph; space/b scrolls the panel, top-anchored, when an entry runs long.
- try: relaunch hyper (a running hyper reads its own code only at launch), press w, then enter on hypercore. The titles sit left under their sigils, the detail panel right — move j/k and watch the panel track the selection and the tags mark each kind; drop into the trak-backend graph (now short-titled) and its nodes read clean.
- state: awaiting acceptance
- blocks: nothing compounds — the change is on disk and reverts in one git command; until you accept, it stays machine-owned material and the old single-column list is one revert away.

