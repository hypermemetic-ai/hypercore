# work

## archived graph lines wear their root's check

- ask: "a folded graph can be trusted only as far as those checks reach" is ratified, but the work view never showed it — a folded graph's line said only "folded · N nodes", and reading how far the fold could be trusted meant opening the graph. now every archived graph line carries its root's check beneath it, dim, in the work view — fold trust reads from the list.
- try: relaunch hyper (q, then the menu) — w, enter on hypercore: graph-machinery's line now shows "check: the operator accepts the slice from its acceptance card" under it; the hyper open now wears the old code
- blocks: until accepted, fold trust stays invisible in the list — every archived graph reads alike, however weak its checks
- state: awaiting acceptance
- since: 2026-06-11
