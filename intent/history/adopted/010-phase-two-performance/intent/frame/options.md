# options - 010-phase-two-performance

Direction options are drafted by the machine for operator selection. The operator
selects one route, rejects all options, or aborts without writing direction.

## option 1

id: full-performance-bundle
kind: selected-route
summary: Carry the full performance proposal as one loop: legible acceptance artifacts,
strong-review floor, fast builder routing, builder retry then escalation, resumable
execute, parallel tier-two panel, phase-one review crash fix, stale-lens verification,
and fake-acceptance hardening; delegate cache-key and retry-detail choices to the
machine under the signed constraints.
reversibility: one-way
tradeoff: Maximizes one-pass performance relief and keeps the proposal coherent, but
changes the whole phase-two hot path at once and therefore requires broad proof-floor
coverage.

## option 2

id: door-cure-first
kind: selected-route
summary: Land the review legibility and door-integrity cure first: structured rationale
and evidence for acceptance, fake-acceptance rejection in real execute/archive,
phase-one review crash fix, stale-lens verification, and parallel tier-two panel; defer
builder routing, retry escalation, and resumable execute to later work.
reversibility: one-way
tradeoff: Narrows the first blast radius around proof integrity and reviewer signal, but
leaves the largest wall-clock multipliers mostly in place until follow-up work.

## option 3

id: resumability-first
kind: selected-route
summary: Land the review legibility, fake-acceptance hardening, resumable execute cache,
and parallel tier-two panel first; keep all models strong and defer the fast-builder
routing plus retry-then-escalate ladder.
reversibility: one-way
tradeoff: Directly attacks blind reruns and serial panel time while avoiding model-routing
change in the first pass, but does not test the proposed cheap-builder proof-floor rule.

## rejection choices

none: The operator may reject all options and send the work back to frame.
abort: The operator may abort without writing direction.
