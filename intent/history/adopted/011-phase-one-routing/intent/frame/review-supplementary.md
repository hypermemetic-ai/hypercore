# supplementary review - 011-phase-one-routing

Not the governed roster artifact (`review.md`). This is an additional, independent
decision-surface review run because the governed roster's base prompt expects a *signed,
route-settled* frame, while one-way phase-one review runs *before* direction and route by gate
design — so all four base reviewers reduced to the same staging artifact ("route is TODO / no
direction or signoff") and could not give substantive signal. This review was prompted to treat
the missing route/direction/signoff as expected and to scrutinize the decision surface itself.

- Reviewer: independent read-only session, strong model (gpt-5.5 @ xhigh), `approval never`,
  `sandbox read-only`. Source: real reviewer, supplementary (not the governed `review.md`).

## findings

- SUBSTANTIVE — "de-name the whole contract" is only coherent if current-binding facts are
  separated from methodology claims; some adapter statements name load-bearing harness mechanics,
  not incidental wording. (→ folded: three-layer model in constraints.)
- SUBSTANTIVE — the product-token acceptance check is under-specified at the
  statement/materialization boundary; the pointer exception needs an exact grammar or it is
  false-positive-prone or a loophole. (→ folded: observable acceptance + route deliverable.)
- SUBSTANTIVE — the proof-floor exclusion is not tight enough around defaults; today the review
  floor rides an ambient harness default, not a checked pinned strong model, so "held default
  reproduces current behavior" may preserve an unchecked default rather than a proven floor.
  (→ folded: softened constraint + route decision.)
- SUBSTANTIVE — "not a second phase-two executor" should also forbid the phase-one collaborator
  from producing phase-two plan/build artifacts; otherwise "phase-one labor" can drift into an
  unsigned planner/executor adjacent to the queued two-step planner. (→ folded: excluded
  interpretation.)
- SUBSTANTIVE — some product-named statements resist simple role/config rephrasing because they
  encode harness capabilities (entry-point loading, instruction-chain inheritance, structured
  exec-event streaming, session-state preflight); they need capability-requirement statements
  plus product-specific materialization proof. (→ folded: capability-requirement layer.)
- SUBSTANTIVE — the sweep/check scope must avoid non-contract product tokens: adopted history,
  scratch findings, child-node names like `codex-cockpit`, and mounted-entrypoint material are
  legitimate corpus text, not target contract statements. (→ folded: scoped check.)
- SUBSTANTIVE — option 2 collides with the queued two-step builder-routing work unless
  alias/default precedence is nailed down now. (→ folded: option 2 tradeoff.)
- MINOR — the two options are materially distinct only at the material env-interface layer; both
  share the same contract rewrite. (Acknowledged; the fork is genuine but narrow.)
- MINOR — `hypercore.md` and `intent/organizing-document.md` carry product-named
  summary/materialization text outside the originally listed check scope. (→ folded: pulled into
  the enumerated check set, treated under the pointer grammar.)

OVERALL: coherent direction surface; adoption should block unless the route makes the
statement/materialization grammar exact and hardens the review-floor and phase-one-collaborator
boundaries.
