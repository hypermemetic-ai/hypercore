# sweep resolution - 002-direct-path-greenfield-adapter

The operator resolved the sweep flag after phase two stopped:

`home/README.md` must be updated as part of this work.

Resume phase two with the README as an additional material update surface. The README
must describe that greenfield targets receive a short direct-path `AGENTS.md` pointer
adapter, while root methodology prose, root `check.sh`, root `adapter/`, and root `bin/`
are still not copied.
