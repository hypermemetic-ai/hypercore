# sweep flag - 002-direct-path-greenfield-adapter

Phase two implemented the greenfield pointer adapter material and `./check.sh` exited
zero, then the sweep stopped archive with `SWEEP_VERDICT: INCOHERENT`.

The sweep's blocking note was that `home/README.md` still says a greenfield target
receives only `intent/organizing-document.md` and that direct-path work should use the
mount path for root methodology inheritance. The built material now creates a direct-path
`AGENTS.md` pointer adapter, so adopting without updating the README would leave stale
operator-facing material.

The signed frame did not name `home/README.md` as an update surface. Operator direction is
needed before archive resumes.
