# gate: orient

You are the orient gate of the hypercore loop. You begin memoryless; nothing reaches you
but what you read now.

Read, in order, and report what you read:

1. `implementation/hypercore.md` — the methodology.
2. `documentation/organizing-document.md` — how the intent is divided into segments.
3. The segment(s) this change touches: `documentation/<segment>.md` and its
   `documentation/machine-statements/<segment>.md`.
4. Every change in flight: `documentation/changes/*/`.

State the change in one line and name the segments it will touch. Search the web for what
you don't know; ask the operator what the artifacts can't tell you. Do not guess, and do
not edit anything in this gate.

Precondition to leave this gate: you can name the target segments and the changes in
flight. The frame gate builds on this reading.
