---
kind: ask
state: done
owner: operator
created: 1782638347
---
The integrate-stage coherence judgment false-refuses a gate-proven build — codex's coherence reply intermittently omits (or returns false for) the `coherent` envelope flag, so a build whose deterministic gate is already green is rejected at integrate as incoherent and the crossing cannot fold without a human architect overriding the judgment by hand.

The integrate path treats the model's coherence reply as authoritative over the deterministic gate. `communication.integrate` asks the architect transport for a coherence envelope (`COHERENCE_SCHEMA`, `engine/communication.py:83`) carrying a `coherent` flag (`Flag("coherent", "true if the result honors the contract")`, `:86`); at `:154` `if not verdict.get("coherent"):` it raises a refusal card. `verdict.get("coherent")` returns a falsy `None` when the reply OMITS the flag, so a malformed or absent coherence reply is indistinguishable from an explicit `coherent: false` — both collapse to "refuse, raise a card." But by the time integrate runs, the build is already proven sound deterministically: the scenario gate cleared red->green (`conditions.verdict` / `scenario.gate`) and the whole-system re-verify is green (`scenario.reverify`). A flaky model envelope at integrate can nonetheless override that proof and discard a ~20-minute verified build.

Evidence (this session's dispatch, 2026-06-27/28): BOTH fenced crossings hit it. The depth pair burned ~5 attempts on `the-model-driven-depth-scan` largely to this flake; `communication-mastery` hit it once. Each folded only because the architect sub-thread re-ran the coherence judgment itself against the actual built diff — keeping every gated check and the whole-system re-verify intact — and folded by hand. So `worker.run`'s AUTONOMOUS integrate path currently cannot fold past this flake without a human in the architect seat: it is the live blocker between the watchably-driven dispatch shape and hands-off autonomy. Same family as the folded `the-fold-s-re-verification` (a perf timeout silently reclassified as a broken build) and `worker-run-is-not-total` (a raise loses its card) — a sound build discarded by a non-correctness fault on the integrate path.

To surface in grilling: whether a MISSING / malformed `coherent` flag must become a DISTINCT outcome (a retry, or an honest "coherence reply unreadable" surfaced to the operator) that never silently equals an explicit `coherent: false` and never discards a gate-proven, re-verified build — exactly the "a timeout must be a distinct outcome" shape `the-fold-s-re-verification` settled; whether the deterministic gate (scenario red->green + whole-system re-verify) should be AUTHORITATIVE so a build whose gate is green cannot be refused as incoherent by the model envelope at all (the model judgment narrowing to what the gate cannot mechanize — the operator-altitude contract read — never re-litigating a mechanically proven behavior); where the coherence authority lives is the load-bearing seam — the deterministic gate versus the model judgment. A red->green scenario: an integrate whose coherence reply omits the `coherent` flag (a malformed/absent envelope over a gate-proven build) MUST NOT fold-refuse as incoherent — it retries or surfaces a distinct unreadable-reply outcome — while an explicit `coherent: false` still raises its card; red on today's `communication.py:154`, green on the fix.
