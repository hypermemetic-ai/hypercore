surfaced: 0

[Q] The ask's deeper half — "give the ratchet ratified intent so the behavior has a single owner" — is already met: folding-conditions.md and architecture-review.md carry the ratchet's behavior as ratified requirements (bounded acceptance, stale-bar re-raise, single durable seam, the accepted-module-is-not-debt scan), no [machine] markers, landed before this node was filed. So the live defect narrows to one drifted reader — the acceptance harness's self-check (engine/check/scenarios.py:108) hand-re-encodes the debt definition and wrongly counts an `accepted` file as unclean. How far should the fix reach?
lean: cure the class — rewrite the self-check to derive "no strong debt" from review.findings (the one debt definition `_finding` builds), so it inherits the architecture-review scenario that already gates accepted→none and cannot re-drift; add no new spec, because the ratchet is already owned and restating it is the duplication the system fights.
flip: patch the instance (drop "accepted" → ("over","exceeded")) if review exposed no clean debt list to reuse — but it does (review.findings); add a new ratified requirement pinning the self-check to the one debt definition only if the operator judged the self-check itself deserves an explicit spec owner.
answer: cure the class — derive from review.findings, no new spec (operator ratified, 2026-06-26).

[CONTRACT]
The acceptance harness's self-check on hypercore's own engine tree no longer keeps its own copy of the debt definition. It reads `review.findings` — the single definition `review._finding` builds — and refuses only on **strong** debt (a module past the length signal with no accepted-length record, or one past a stale bar), so a module carrying a justified accepted-length record keeps the harness green: the settled decision is honored the same way the fold gate (`conditions.accepted`) and the operator backlog (`review._finding`) already honor it. Because the self-check now reads `_finding`, it inherits the architecture-review scenario `accept deep.py @460 → debt none` that gates that mapping, so this reader can no longer drift from the other three. No spec changes: the ratchet's behavior is already ratified in folding-conditions.md and architecture-review.md, and adding a fourth restatement would reintroduce the hand-maintained duplication that caused the drift. Validated by: the self-check predicate goes red→green on a constructed `accepted` module while staying red on `over`/`exceeded` (the regression guard for real god-files), and `python3 -m engine --check` is green.

[DELTA]
Trivial — no spec verbs. The change touches only the acceptance harness (engine/check/scenarios.py), not any capability's behavior; the ratchet's behavior is already owned by folding-conditions.md and architecture-review.md, and the corrected mapping (accepted → not debt) is already gated by the existing architecture-review scenario `accept deep.py @460 → debt none`. So there is no ADDED/MODIFIED/REMOVED/RENAMED requirement: the fold lands the harness fix and archives the node, carrying no spec delta.
