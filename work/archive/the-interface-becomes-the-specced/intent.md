---
kind: ask
state: abandoned
owner: operator
created: 1782668378
---
> Abandoned 2026-06-29 — superseded by `the-interface-leaves-the-terminal`: the
> owned-window rebuild retires the curses interface this tree was building up.

The interface becomes the specced operator surface — carve intent.md §36–62 into spec/interface.md (and one queue requirement) and build the window up from a terminal stub to the decision surface intent describes.

Ratified contract (operator, 2026-06-28): the operator's window owns its own ground and small palette (warm light ink on a soft-dark ground by default, light the toggle — amended from the original warm-dark-on-off-white by the operator on 2026-06-28), never inheriting the terminal; color names a kind and never carries a state alone; the standing work renders as the tree with a scent on every folded node (children, shape, pass/fail) and state legible from the glyph first, a single slow pulse on running work; a decision card carries its full anatomy — synthesis headings, the options and what each entails, the cost of its delay, the lean and the one flip, confirming detail one keystroke below; and a key on any element tied to a model's working opens a live reasoning loop — the architect's own thread fully steerable, a fenced worker's trace shown read-only with acting at the tree grain (prune, re-ask, rerun). Substrate floor: owned terminal grid, truecolor. Out of scope: GUI/web canvas + chosen typeface; live mid-run injection into the worker's session (node-grain only).

Decomposition (as-needed, serialized — every slice touches render.py/interface_world.py): (1) the owned ground + the color law; (2) work renders as the tree, with scent + glyph-first state; (3) the decision-card anatomy (queue + interface); (4) the node-grain reasoning loop. The seams after (1) are cut as the work reaches them.

Folding condition: the interface renders as the specced surface — each slice's gated scenarios go red→green and the watched facts (the painted ground, the running pulse, the live reasoning stream) are confirmed on the running window; python3 -m engine --check is green.
