---
kind: ask
state: done
owner: operator
created: 1782668378
---
Interface slice 1 — the window owns its ground and the color law. The frame stops inheriting the operator's terminal: it declares its own ground (warm dark ink on off-white paper, a soft-dark polarity the operator toggles) and a small named palette with one alarm hue in reserve, all computed without any terminal default; and no state the frame shows rides on color alone — every state hue shares its row with a glyph or word, so stripping color leaves every state legible. The foundation the rest of the interface paints on.

Folding condition: spec/interface.md carries the two requirements (owned ground+palette; color names a kind) with their scenarios; the gated checks (ground/palette/polarity and the redundant-cue check) go red→green; python3 -m engine --check is green.
