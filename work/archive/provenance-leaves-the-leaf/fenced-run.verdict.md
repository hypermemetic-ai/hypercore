# fenced-run — watched verdict [machine]

fenced worker crossing folded

node: provenance-leaves-the-leaf
subject: Provenance leaves the leaf — extract the ledger primitive that `conditions` and `provenance`
