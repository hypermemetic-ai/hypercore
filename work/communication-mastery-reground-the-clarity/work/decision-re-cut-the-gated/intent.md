---
kind: decide
state: awaiting you
owner: machine
created: 1782630648
---
Decision — re-cut: the gated caveat-survival routing.

This hand-off folds nothing. The claimed work — the survival guard in the archive render, the world-fixture verbs that exercise it, and the new requirement "a dropped caveat never reaches the operator" — is absent from the repository, and the report's "check is green" is contradicted by the gate: the named scenario does not exist in the acceptance run. The hand-back is the only artifact, and it can't be trusted on its own verification.

The ask is sound and so is its design — the entailment verdict watched, the routing gated against a scripted oracle. Rebuild it fenced and require the hand-back to carry the actually-verified delta and code, re-checked green on merge.

Before re-cutting it standalone: the in-flight "reground the clarity standard" ask already stages this exact scenario and requirement in its draft. Decide whether caveat-survival folds into that work instead of running as its own crossing.

[machine]
