---
kind: decide
state: awaiting you
owner: machine
created: 1782601084
---
Re-cut: architect-prompts-route-to-skills. The hand-off reported success, but nothing landed — the working tree sits unchanged at the dispatch commit. Verified directly: design.SELECT still carries DEPTH/LOCALITY/SEAM PLACEMENT and names no skill; scenarios.py still holds the old axes assertion (the contract's red→green guard and the six-prompt routing assertion are absent); communication.SYSTEM/COHERENCE and grill.FLOOR/PRODUCTS still restate methodology with no skill named; there is no EXPLAIN constant. `python3 -m engine --check` passes vacuously because the new oracle was never cut, so the loop has no teeth for this change. Re-dispatch the build off current main; treat the prior report as unverified.

[machine]
