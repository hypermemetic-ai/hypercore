---
kind: ask
state: in flight
owner: operator
created: 1782584395
---
The live-run gate sees the crossing — the live-run signal flips when a real fenced `worker.run` crossing folds, so #11's gate reads fenced evidence the dispatch path actually produces, not an autonomous-run trace it never writes.

The dispatch system and the hand-cranked snapshot both claim the first live fenced crossing produces the watched evidence the rest of the system is waiting on — `.claude/commands/dispatch.md` Step 3 ("that first crossing is also the watched evidence the rest of the system is waiting on") and `the-hand-cranked-dispatch/dispatch.md` #11/#12 ("the first live run produces the watched evidence #11 is gated on"). It does not. The first live crossing (`worker.run` on `the-candidate-prompt-speaks-as-the-architect`, folded `bfcf9e7`) ran codex in the fence and folded green, yet `view._live_status` (view.py:102) still reads `never-run-live`. The gate is `_has_watched_trace` (view.py:108-111): true iff some node folder holds a `*.verdict.md`, which only `provenance.commit_verdict` writes — and that is called solely by the watched mechanisms (the vocabulary check, the depth scan) and the `self_model_world` fixture's `autonomous-run` verdict, never by `worker.run`. So a routine fenced fold leaves no such trace, and `never-run-live` cannot flip by dispatch however many crossings run. The seam conflates two distinct things: a *fenced* crossing (codex built it, watched by the operator) and an *autonomous* run (the machine self-dispatched, leaving the `autonomous-run` verdict). #11 (`worker-disciplines-become-a-loadable-skill`) is gated on the former in prose but the latter in code, so it stays gated regardless of how the loop is driven.

Build it so the signal tracks reality. The live-run gate reflects what actually happened — a fenced `worker.run` crossing that built on codex and folded leaves a durable trace the gate reads. Either `worker.run`/the integrate writes a fenced-run verdict on the node it folds (the writer side the gate already expects), or the gate is split so "a fenced crossing has run" and "an autonomous run has occurred" become distinct signals the operator and #11 each read the right one. The dispatch command's Step 3 and the snapshot's #11/#12 claims are corrected to match whatever the seam actually measures.

To surface in grilling: the load-bearing fork — does "live-run" mean *fenced* (codex built it, regardless of who pressed go) or *autonomous* (the machine dispatched itself, no operator in the loop)? #3 was fenced-but-operator-driven, and whether that should flip the gate is the decision #11 hangs on. Then: whether `worker.run` writes a verdict trace on its fold (mechanism name, what the verdict attests) and how that composes with the existing watched-trace readers (vocabulary check, depth scan) so distinct traces are never conflated; whether the dispatch command and snapshot claims are corrected in the same pass or filed as doc-debt; and a red→green scenario that dispatches a fenced crossing and asserts the live-run signal flips.
