# the small machine hyper summons for busywork — today: truing up a
# record left behind.  same format as the machine file: one command,
# one visible setting, {prompt} standing for the task.  option 3 of the
# routing card (operator's pick, 2026-06-11): the same model as the full
# machine at low effort — one model, one cache, one behavior, most of
# the speed, no cross-model risk.  the bar it runs under is hard and
# lives in the prompt hyper hands it: commit what is actually on disk
# with grounds faithful to the diff, never finish or correct half-done
# work; anything ambiguous it leaves uncommitted, named, for the full
# machine.  HYPER_MINI in the environment overrides this file (testing);
# with no mini file, the full machine takes the busywork itself.
claude -p {prompt} --model claude-opus-4-8 --effort low --dangerously-skip-permissions
